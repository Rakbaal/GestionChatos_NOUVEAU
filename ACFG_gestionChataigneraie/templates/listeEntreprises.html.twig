<!DOCTYPE html>
<html>
	<head>
		<title>Liste Entreprises</title>
		<meta charset="UTF-8">
	</head>

	<body>
		{{include('navbar.html.twig')}}
		<table>
			<tr>
				<td>ID</td>
				<td>Raison Sociale</td>
				<td>Adresse</td>
				<td>Pays</td>
				<td>Spécialité</td>
			</tr>
			{% for entreprise in listeEntreprises %}
				<tr>
					<td>{{ entreprise.getId() }}</td>
					<td>
						<a href={{ path('infoEntreprise', { 'id': entreprise.getId() } ) }}>
							{{ entreprise.getENTRS() }}</a>
					</td>
					<td>{{ entreprise.getENTADRESSE() }}
						<br/>
						{{ entreprise.getENTCP() }}
						{{ entreprise.getENTVILLE() }}</td>
					<td>{{ entreprise.getENTPAYS() }}</td>

					{#<!-- <td style="width: 250px; border: 1px solid black; text-decoration : none; color: black;">
                            {% for personne in entreprise.getPersonnes() %}
                                {{ personne.getPERNOM() }} {{ personne.getPERPRENOM() }} 
                                {{ personne.getPERMAIL() }} , {{ personne.getPERTEL() }} 
                            {% endfor %}
                        </td>
            
                        -->
                        #}

            {#
            <!--

            <td style="width: 250px; border: 1px solid black; text-decoration : none; color: black;">
                {% for personne in entreprise.getPersonnes() %}
                    {{ personne.getPERNOM() }} {{ personne.getPERPRENOM() }} 
                    {{ personne.getPERMAIL() }} , {{ personne.getPERTEL() }} 
                {% endfor %}
            </td>

            -->
            #}

            <td>
                {% for personne in entreprise.getSpecialites() %}
                    {{ personne.getSPELIBELLE() }}
                {% endfor %}
            </td>
            <td>
                <a href={{ path('supprimerEntreprise', { 'id': entreprise.getId() }) }}>X</a> 
            </td>
            </tr>
        {% endfor %}
    </table>
</div>

<div>
    <table>
        <tr>
            <td>Rechercher par filtres :</td>
        </tr>
    </table>
    {{form(formFiltre)}}
</div>

<br/>
<br/>
<br/>

{% if admin %}
	<div>
		<h1>Création d'une entreprise :</h1>  
		{{form(formNouveau)}}
	</div>
    {% endif %}

